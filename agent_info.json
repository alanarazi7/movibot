{
  "description": "MoviBot recommends up to 3 movies from a curated 238-film Disney/Pixar catalog (feature films, 1940-2017) for requests that mix hard constraints with soft ones. It is a ReAct agent: an LLM planner reasons, calls tools, observes what came back, and decides whether to go again or answer. All four tools are available on every turn and none is mandatory, so the planner decides which the question actually needs and stops as soon as it can answer. CatalogFilter answers from the catalog's own columns. Its full argument list is year_min/year_max (release year), genres and exclude_genres (ALL required genres must match; any excluded genre rejects), keywords (case-insensitive substring match against each film's curated keyword column, ANY may match -- a real column, but a text list rather than a controlled vocabulary, so it is a broad topical filter and not proof of a theme), studio (substring), languages and exclude_english_only (spoken language), exclude_titles (exact-title drop), require_synopsis (keep only films whose full plot text can be read later), and list_all (return every matching title rather than the best few). Every argument reads a stored column, so the tool is free, exact and exhaustive over the catalog. LexicalScreen tests an absence a word list can express -- \"nobody dies\", \"nothing scary\" -- by scanning every plot passage of every candidate, which ranking cannot do; searching for \"nobody dies\" would instead return the films where somebody does. It is exhaustive over the word list, not over the event: a clear result means no listed word appears in the stored text, which an event can evade by being narrated in other words or omitted entirely. The agent reports it in those terms and escalates to SynopsisReader when a negative condition matters and the screen left it unresolved. PlotSearch answers from meaning, ranking films by semantic similarity over full plot synopses. SynopsisReader returns the full plot text so the planner can judge claims that no column encodes, such as who betrays whom.\n\nWhat it CAN do: combine structured and thematic constraints in one query; honour exclusions (\"besides Frozen and Moana\") as real filters rather than as omissions from the prose; verify story-level properties by reading the plot rather than guessing from genre; and explain why it rejected the obvious alternative.\n\nWhat it CANNOT do: reason about films outside its catalog (it is a curated Disney/Pixar subset, not all of IMDb); answer anything about films released after 2017, because the underlying dataset is a 2017 snapshot -- it has no knowledge of recent, current, or trending releases and is instructed to refuse such questions rather than answer them from a stale catalog; recommend a short, since everything under 45 minutes was excluded; support cast or crew as a reliable constraint: there is no structured cast or crew index, so it cannot enumerate an actor's or director's films and will not claim an exhaustive match. Cached Wikipedia context text does carry cast and production material for many films and can surface in search, so a passing mention is possible -- that is a mention, not an index, and it is not treated as one; and it does not stream or play content. Every film in the catalog is semantically searchable, but the underlying text is uneven: most have full plot text that SynopsisReader can read, while a small number have only a short overview. Where a film's stored text is too thin to settle a story-level claim, the agent says the check could not be performed rather than guessing. Where a request is answerable but narrower than the user assumes -- 'recommend a nice comedy' -- it names the universe it is drawing from before recommending, rather than passing a Disney-only pick off as a survey of all cinema.\n\nOn completeness: with only 238 films the agent could in principle check every one, but verifying story-level claims means reading full plot texts, so an exhaustive pass would cost far more than any recommendation is worth. Two of its four tools are genuinely exhaustive, though: CatalogFilter and LexicalScreen both check every candidate, so their findings are complete rather than sampled. The other two are not -- PlotSearch returns a top handful and SynopsisReader reads at most 8 plots per call -- which is exactly why the planner is told to prefer the free exhaustive tools first, shrinking the set before the expensive approximate ones see it. It is instructed to say which kind of finding an answer rests on: a shortlist is the best among the films it looked at, not the best in the catalog. MoviBot names at most 3 films in any answer. That is a hard ceiling, not a default: no request raises it, including one that asks for everything. Such a request is answered with the ceiling stated plainly -- this demo does not return complete lists -- and, where the surviving set was settled entirely by the two exhaustive tools, with the true match count, so the user learns how large the answer is without being handed a list. Where PlotSearch or SynopsisReader contributed there is no true count, and the agent says the films are the best among those it examined rather than the best in the catalog.\n\nTwo ranking guardrails are enforced in the data and tool code rather than in the prompt, so no plan can bypass them: shorts under 45 minutes are removed at data-preparation time (they carry very high ratings on very few votes and otherwise dominate every result), and ranking uses a vote-count-weighted Bayesian rating instead of the raw average.",
  "purpose": "Answer the kind of movie request that a filter alone cannot: one that combines facts the catalog stores with judgements only the story can settle, and return up to 3 defensible recommendations with the reasoning shown.",
  "prompt_template": {
    "template": "Query: <a movie request in natural language. It may mix structured constraints (era, genre, studio, language, titles to exclude) with story-level ones (theme, tone, what must or must not happen in the plot, who it is for).>",
    "example": "Query: Best empowering princess movie besides Frozen and Moana."
  },
  "architecture": {
    "type": "ReAct agent (reason -> act -> observe -> stop), native tool calling",
    "planner_module": "Planner",
    "tool_modules": [
      "CatalogFilter",
      "LexicalScreen",
      "PlotSearch",
      "SynopsisReader"
    ],
    "max_model_turns_per_request": 5,
    "notes": "Module names here match the boxes in /api/model_architecture and the `module` field of every entry in the /api/execute steps trace. Three of the four tools are entirely free and the fourth costs one embedding call (~$0.0000002), because all of them run against local data; only the planner turns cost anything, which is what the turn bound exists to cap. The candidate set is held in Python between calls and never enters the prompt, so films are addressed by \"Title (Year)\" rather than by id.",
    "tool_order": "A heuristic preference, not an enforced invariant, and the distinction matters for cost and reproducibility. The system prompt encourages cheapest-and-most-exhaustive first -- CatalogFilter narrows 238 films to N on columns; LexicalScreen splits N into clear/flagged/insufficient_text on a word scan; PlotSearch ranks what remains; SynopsisReader reads at most 8 -- because when that order is followed each call hands the next a smaller candidate set, so the token-heavy tool only ever sees what survived the free ones. The planner MAY reorder, skip, or repeat tools, and does skip any step whose kind of condition a request does not contain; two runs of the same query are not guaranteed to produce the same tool sequence. Nothing in the code enforces the order. What the code does enforce are the hard bounds: max_model_turns_per_request, the per-call read and result caps, the 45-minute runtime floor, and vote-weighted ranking.",
    "condition_types": {
      "_routing_rule": "Conditions are routed by the evidence that would settle them, never by how the user phrased them. A negation is not a category: 'not Pixar' is a column lookup, 'nobody dies' is a word test, 'not depressing' is a concept that needs reading.",
      "structured": "a fact the catalog stores, including its negative form -> CatalogFilter. 'not Pixar', 'no musicals', 'besides Frozen' and 'nothing before 2000' are settled exactly and for free by studio, exclude_genres, exclude_titles and year_min. They never reach a screen or a search.",
      "lexical negative": "an absence a concrete word list can test -- 'nobody dies', 'nothing scary' -> LexicalScreen first. Not PlotSearch, because searching for 'nobody dies' returns the films where somebody does.",
      "semantic": "a story, premise or theme stated positively -> PlotSearch",
      "narrative": "a claim needing to know what happens, including whether a screen's word match was a real event -> SynopsisReader",
      "_escalation": "Negatives that are concepts rather than vocabularies -- 'not depressing', 'doesn't focus on romance' -- are handled as semantic or narrative conditions. Nothing prohibits PlotSearch or SynopsisReader from settling a negative: when a lexical screen leaves one unresolved, reading is the way to resolve it, and any residual uncertainty is disclosed in the answer."
    },
    "control_flow": "A single bounded ReAct loop in agent/loop.py. Every non-final turn offers all four tool schemas; the planner chooses which tools to call, with what arguments, and how many in one turn. Tool results are appended and the loop returns to the planner, which either calls again or answers. The loop exits when the planner emits no tool call, so stopping is the model's decision; max_model_turns_per_request is a spending cap, not the normal exit. No code path sequences the tools, and no tool is mandatory: a request answerable by CatalogFilter alone never reaches the other three.",
    "condition_types_note": "A rule about evidence, not about order: it fixes which tool can settle a given kind of condition, never which condition to take up first, how many turns to spend, or whether to go back and re-query."
  },
  "prompt_examples": [
    {
      "prompt": "Best empowering princess movie besides Frozen and Moana.",
      "expected_tool_path": "Planner -> CatalogFilter(keywords=['princess','royalty'], exclude_titles=['Frozen','Moana'], require_synopsis=true) -> Planner -> Final Answer",
      "verified_tool_output": "CatalogFilter matched 13 films, returned best-first by weighted rating: Mulan (1998, 7.42), Beauty and the Beast (1991, 7.38), Aladdin (1992, 7.31), Tangled (2010, 7.30), The Little Mermaid (1989, 7.06). Frozen and Moana are absent because the exclusion was applied as a filter, not left to the prose.",
      "status": "Tool path verified locally at zero cost. The final natural-language response will be captured from the first real planner run once the LLM endpoint is enabled."
    },
    {
      "prompt": "Something safe for a toddler where nobody dies.",
      "expected_tool_path": "Planner (writes the condition ledger in the same message) -> CatalogFilter(genres=['Animation']) -> LexicalScreen(vocabulary='death') -> Planner -> Final Answer",
      "verified_tool_output": "CatalogFilter matched 90 animated films. LexicalScreen then scanned every plot passage of all 90 for a 37-word death vocabulary and split them: 27 clear, 61 flagged, 2 with too little plot text to screen. The best-rated clear films are Inside Out (7.83), WALL-E (7.73), Toy Story 3 (7.52). The Lion King and Up are flagged, which is correct -- Mufasa and Ellie die, and neither fact appears in any genre, keyword or overview field. Note that flagged means unresolved rather than rejected: a match is often an attempt or a false belief (\"believing Woody murdered Buzz\"), which is modality rather than vocabulary, so the matching quote comes back with the flag. Two model turns, no embedding call and no synopsis read.",
      "status": "Tool path and every figure above verified locally at zero cost, and asserted by scripts/check_screen.py. The final natural-language response will be captured from a real planner run."
    }
  ]
}
